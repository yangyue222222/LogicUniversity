<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.logicuniversity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".DisburseList">
        </activity>
        <activity android:name=".DisburseOrder">

        </activity>
        <activity android:name=".StationeryRetrievalForm">
        </activity>

        <activity android:name=".DeptActivity">
        </activity>

        <activity android:name=".DeptApprovalsList">
        </activity>
        <activity android:name=".DeptApproveRequest">
        </activity>
    </application>

</manifest>